<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean id="queries" class="java.util.HashMap">
        <constructor-arg>
            <map>
                <entry key="addDepartment" value="insert into department(name) value(:p_name)"/>

                <entry key="departmentGetById" value="select id department_id, name from department where id = :p_id"/>

                <entry key="getAllDepartment" value="select id department_id, name from department"/>

                <entry key="addProduct" value="insert into product(product_name, price, department_id) values(:p_name, :p_price, :p_departmentId)"/>

                <entry key="getAllProduct" value="select product.id product_id, product.product_name, product.price, product.department_id, department.name from product inner join department on product.department_id=department.id"/>

                <entry key="productGetById" value="select product.id product_id, product.product_name, product.price, product.department_id, department.name from product inner join department on product.department_id=department.id where product.id = :p_id"/>

                <entry key="addOrderWithoutId" value="insert into order (product_id, quantity, status) value(:p_product_id, :p_quantity, :p_status)"/>

                <entry key="addOrderWithId" value="insert into order (id, product_id, quantity, status) value(:p_product_id, :p_quantity, :p_status) where id = :p_id"/>

                <entry key="getAllOrder" value="select order.id, order.product_id, order.quantity, order.status, product.product_name, product.price, product.department_id,department.name from order inner join product on order.product_id=product.id inner join department on product.department_id=department.id order by order.id, product.id"/>

            </map>
        </constructor-arg>
    </bean>

</beans>